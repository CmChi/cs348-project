# cs348-project

The backend (flask) is included in the root directory, and the frontend (react) is in the UI folder. I developed on a Postgres DB (the url is still present in the backend files) from docker so no DB files are included. Lots of endpoints are commented out since they didn't end up being used.
